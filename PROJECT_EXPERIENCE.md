# 项目经历写法

## 金融付款自动化 Agent 平台

**技术栈**：Python、Playwright、TypeScript、Node.js、SQLite、Feishu Bot、Codex CLI、Claude Code、PowerShell、Windows GUI Automation、USBHub/UKey 控制

**项目简介**：

设计并实现一套面向财务付款流程的桌面自动化 Agent 系统，打通 OA/M3 付款数据抽取、银行路由、银行制单执行、飞书机器人远程调度与失败上报。系统默认运行在测试/验证模式，生产路径采用多层显式开关、字段校验、跨项目互斥锁和 fail-closed 策略，避免误制单和重复提交。

**核心工作**：

- 搭建 M3/OA 数据抽取流程，自动登录、进入财务报表、扫描最新付款记录，提取申请人、申请部门、金额、合同信息、收款户名、账号、开户行、单据编号等字段。
- 设计付款银行识别与路由逻辑，将付款记录按招行、兴业、农行、中行生成队列，并按 M3 调度顺序跨银行执行。
- 实现银行侧自动化执行器，包括余额查询、转账表单填充、制单提交到待审核/待复核队列、USBHub 端口控制、UKey/K宝窗口处理和运行产物清理。
- 开发飞书 Agent 网关，支持 `/start`、`/run`、`/status`、`/approve`、`/cancel`、截图、图片/文件回传、视频抽帧和自动化失败上报。
- 建立资金流程安全边界：默认 fill-only/dry-run，生产需要显式开关；账号、金额、银行、截图、付款单位映射等任一异常均停止；超时/锁忙/疑似重复提交场景不自动重跑。
- 建立运维能力：watchdog 守护、崩溃恢复、全局银行自动化锁、日志/截图保留策略、敏感文件扫描、脱敏上报和财务同事可读的失败摘要。

**项目亮点**：

- 将聊天机器人、浏览器自动化、Windows GUI 自动化、硬件 UKey 管理和银行流程编排整合成一套可追踪的 Agent 工作流。
- 对资金相关流程采用保守安全设计，明确区分“制单/经办录入”和“最终付款”，系统不自动复核、授权或放款。
- 失败通知面向业务用户输出清晰原因和下一步动作，避免把本机路径、堆栈、stdout/stderr 原样暴露给财务人员。
- 代码仓库包含脱敏后的架构、路由、安全门禁、清理、上报与测试/预检逻辑，可作为复杂桌面自动化项目样例。

**可写进简历的一句话**：

独立搭建金融付款自动化 Agent 平台，打通 OA/M3 数据抽取、四家银行制单执行、USBHub/UKey 控制和飞书机器人调度上报，并通过 fail-closed 校验、显式生产门禁、全局互斥锁和审计清理机制降低资金流程自动化风险。
