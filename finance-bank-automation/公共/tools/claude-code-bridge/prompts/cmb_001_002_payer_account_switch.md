# 任务：给招行制单增加 001 / 002 付款方账号切换能力

你正在修改这个仓库：

`C:\Users\30112\Desktop\财务`

请先阅读：

1. `C:\Users\30112\Desktop\财务\银行自动化项目总览.md`
2. `C:\Users\30112\Desktop\财务\招行\招行制单\招行\skills\制单单账号单笔转账skill\SKILL.md`
3. `C:\Users\30112\Desktop\财务\招行\招行制单\招行\ubank_common.py`
4. `C:\Users\30112\Desktop\财务\招行\招行制单\招行\skills\制单单账号单笔转账skill\制单.py`
5. 相关 `zhidan_*` 模块

背景：

招行 U-BANK 单笔转账经办页的“付款方账号”是一个下拉框，现在同一个登录名下可能有两条付款账号候选：

- `1109 6079 5610 001, 北京巡鲜电子商务有限公司, 北京, 北京巡鲜电子商务有限公司`
- `1109 6079 5610 002, 北京巡鲜电子商务有限公司, 北京, 北京巡鲜电子商务有限公司`

需要在自动填单前支持选择 `001` 或 `002`。截图参考：

`C:\Users\30112\Pictures\Screenshots\屏幕截图 2026-05-20 175542.png`

实现要求：

- 在 `bank_form.json` 中支持可选字段，建议兼容这些别名：`付款方账号尾号`、`付款方账号`、`CMB_PAYER_ACCOUNT_SUFFIX`、`CMB_PAYER_ACCOUNT_TEXT`。
- 如果没有提供这些字段，保持现有行为不变，不主动切换付款方账号。
- 如果提供 `001` / `002` 这类尾号，必须在“付款方账号”下拉候选中选择账号文本里匹配该尾号的唯一候选。
- 如果提供完整付款方账号文本或账号号码，则优先做规范化后的唯一匹配。
- 若找不到候选、候选为空、匹配多条、回读不一致，必须 fail-closed，在填写收款方账号/金额之前停止，不能继续经办。
- 选择动作应发生在已进入 `单笔转账经办` 页面之后、填写收款方信息之前。
- 不得改变提交边界：`ZHIDAN_TEST_MODE=1` 仍只允许测试模式；`ZHIDAN_TEST_MODE=0` 才允许第二次经办。不要新增任何自动复核、授权、确认或最终付款。
- 不得打印密码、证书密码或 UKey PIN。
- 尽量复用现有 UIA / Win32 helper 风格，新增逻辑放进合适的 `zhidan_*` 模块，不要把 `制单.py` 变胖。
- 如果新增 `bank_form.json` 字段或改变招行运行契约，请同步更新 `银行自动化项目总览.md` 和招行 `SKILL.md`。

验证要求：

- 至少运行相关 Python 文件的 `py_compile`。
- 不要真实运行生产提交；不要设置 `ZHIDAN_TEST_MODE=0`。
- 最终回复请说明改了哪些文件、如何在 `bank_form.json` 中指定 `001` 或 `002`、以及 fail-closed 行为。
